const copyPartInto = () => {}

const writeSplitWim = () => {}

const startNewSwmPart = () => {}

const addBlobToSwm = () => {}

const wimlibSplit = (wim: any, name: string, partSize: number) => {
  const ret = startNewSwmPart()

  if (ret) {
    return 0
  }
}
